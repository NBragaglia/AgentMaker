# __AGENT_NAME__

Starter Python CLI agent scaffold.

## Setup

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -e .
pip install pytest
```

## Run

```powershell
__AGENT_NAME__ --help
```

## Test

```powershell
pytest -q
```
